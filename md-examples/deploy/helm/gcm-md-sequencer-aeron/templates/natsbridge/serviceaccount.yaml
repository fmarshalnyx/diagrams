{{- if .Values.natsBridge.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "gcm-md.natsBridge.name" . }}
  labels:
    {{- include "gcm-md.labels" . | nindent 4 }}
    app.kubernetes.io/component: nats-bridge
{{- end }}
